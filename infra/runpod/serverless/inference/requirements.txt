# RunPod Serverless — Foundation Model pipeline dependencies
# No PyTorch/transformers — GPU inference runs via NVIDIA NIM API
# Versions pinned for reproducible production builds

# RunPod serverless SDK
runpod>=1.3.0,<2.0.0

# HTTP client (NIM VLM + OSM Overpass + Esri tiles)
httpx>=0.27.0,<1.0.0
aiohttp>=3.9.0,<4.0.0

# Image processing
Pillow>=10.3.0,<11.0.0
numpy>=1.26.0,<2.0.0

# API interaction (NIM uses OpenAI-compatible chat completions API)
openai>=1.30.0,<2.0.0

# Geo utilities (for building footprint area calc)
shapely>=2.0.0,<3.0.0

# Data validation
pydantic>=2.7.0,<3.0.0

# Production extras
uvloop>=0.19.0,<1.0.0; sys_platform != "win32"
httptools>=0.6.0,<1.0.0
