# RunPod Serverless — Foundation Model pipeline dependencies
# No PyTorch/transformers — GPU inference runs via NVIDIA NIM API

runpod>=1.3.0

# HTTP client (NIM VLM + OSM Overpass + Esri tiles)
httpx>=0.27.0
aiohttp>=3.9.0

# Image processing
Pillow>=10.3.0
numpy>=1.26.0

# API interaction
openai>=1.30.0          # NIM uses OpenAI-compatible chat completions API

# Geo utilities (for building footprint area calc)
shapely>=2.0.0

# PDF quote generation (optional, skip if not needed)
# reportlab>=4.2.0

# Data validation
pydantic>=2.7.0
